[pytest]
# Add your project root to the Python path
pythonpath = .

# Optional: directories where pytest will look for tests
testpaths =
    tests

# Optional: file patterns to recognize as test files
python_files =
    test_*.py
    *_test.py
